import { themeCurrent } from '@lyra/app/vex/theme.entries';
import { notificationsUnseen } from '@lyra/app/vex/tide.entries';

export const themeCurrentPrism = { fingerprint: themeCurrent.fingerprint, context: {} };
export const unseenPrism = { fingerprint: notificationsUnseen.fingerprint, context: {} };
