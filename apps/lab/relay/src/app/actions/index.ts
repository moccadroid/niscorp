// The action catalog — every definition relay ships, aggregated. The shell
// itself runs in moss (built from src/relay.ts's manifest); the browser is
// a canvas terminal (main.tsx).
export { ACTIONS, CATALOG_DEFINITIONS } from '@relay/app/actions/catalog';
