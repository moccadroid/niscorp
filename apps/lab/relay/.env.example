# LLM keys — server configuration (SERVER.md step 4: agents run inside moss).
# Copy to .env and fill in. WHICH model each agent runs on is the roster in
# src/server/llm/index.ts, editable live at Settings → Models; set both keys so
# every model in that roster is reachable.
GROQ_API_KEY=
OPENROUTER_API_KEY=
