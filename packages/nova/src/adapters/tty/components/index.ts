export { defaultRegistry } from './registry';
export { ActionSlot, Badge, Box, Row, Stack, Grid, Text, Button, Input, Checkbox, Textarea, Table, Panel, JsonTree, fallback } from './components';
export { str, num, bool, iconGlyph, stack, inline, inlineText, truncate, pad, indent } from './props';
export type { Props } from './props';
